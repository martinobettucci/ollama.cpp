# ollama.cpp — dépendances runtime + tests.
# Versions ALIGNÉES sur celles d'ollama-gateway (docs/DAT.md §11) : les deux composants sont
# maillons de la même chaîne, deux écosystèmes divergents y seraient une dette immédiate.
# Aucune dépendance d'inférence : llama-server est un binaire externe, non modifié.
fastapi==0.139.2
starlette==1.3.1                # dépendance directe : réponses en flux (NDJSON et SSE)
uvicorn[standard]==0.51.0
httpx==0.28.1                   # client amont vers les instances llama-server, streaming inclus

# --- tests ---
pytest==9.1.1
pytest-asyncio==1.4.0
